# インターネットに公開する手順（GitHub Pages）

このツールを、URLを知っている人なら誰でも使える状態にする手順です。
**全部無料**で、**10分ほど**で終わります。

ファイルはすでに GitHub に上がっているので、**アップロードの作業はありません。**
GitHub の画面で**スイッチを2つ入れるだけ**です。

---

## はじめに知っておくこと

- 公開すると、**サイトだけでなくプログラムの中身も誰でも読める状態**になります。
  中身は点検ずみで、**個人情報もゲームのアカウント情報も入っていません。**
  （実際のスクリーンショット `bari.png` は、はじめから GitHub に上げない設定にしてあります）
- 検索避けの設定（noindex）は**外してあります。** しばらくすると Google などの
  検索結果にも出るようになります。出したくなくなったら、いつでも戻せます。
- **パスワードの入力は、必ずご自身で行ってください。**
  （私＝Claude は、パスワードの入力やアカウントの操作を代わりにできません）

---

## ステップ1：リポジトリを「公開」に切りかえる

いまは非公開（Private）なので、公開（Public）に変えます。
**GitHub Pages は、無料のプランでは公開リポジトリでしか使えません。**

1. ブラウザで次のアドレスを開きます（GitHub にログインした状態で）。

   ```
   https://github.com/akirahaan-blip/pksl.teams/settings
   ```

2. 開いた画面を**いちばん下までスクロール**します。
   赤い枠で囲まれた **「Danger Zone」**（危険な操作、という意味）があります。

3. その中の **「Change repository visibility」** の行にある
   **「Change visibility」** ボタンをクリックします。

4. 小さな窓が出ます。**「Make public」** を選んで、
   下の **「I want to make this repository public」** ボタンをクリックします。

5. 確認のため、**リポジトリ名を打ち込む**よう求められます。
   入力欄に次のとおり半角で打ち込みます（コピーして貼り付けてもかまいません）。

   ```
   akirahaan-blip/pksl.teams
   ```

6. **「I understand, make this repository public」** ボタンをクリックします。

> **うまくいったかの見分け方**：
> ページの上のほうにあるリポジトリ名の右側の印が、
> `Private`（非公開）から **`Public`（公開）** に変わっていればOKです。

---

## ステップ2：公開のスイッチを入れる

1. 同じ Settings の画面で、**左側のメニュー**を下にたどって
   **「Pages」** をクリックします。

   （直接開く場合はこちら → `https://github.com/akirahaan-blip/pksl.teams/settings/pages`）

2. **「Build and deployment」** という見出しの下の **「Source」** が
   **`Deploy from a branch`** になっていることを確認します。
   （なっていなければ、クリックしてそれを選びます）

3. その下の **「Branch」** の欄で、
   - 左のドロップダウン（いまは `None` と出ています）をクリックして **`main`** を選ぶ
   - 右のドロップダウンは **`/ (root)`** のままでOK

4. 右の **「Save」** ボタンをクリックします。

> **うまくいったかの見分け方**：
> 画面の上のほうに
> `Your site is live at https://akirahaan-blip.github.io/pksl.teams/`
> というメッセージが出ます。
> **すぐには出ません。1〜5分ほどかかります。**
> 出ないときは、しばらく待ってからページを再読み込み（F5キー）してください。

---

## ステップ3：ちゃんと見えるか確かめる

公開されるURLはこちらです。

```
https://akirahaan-blip.github.io/pksl.teams/
```

1. そのURLをブラウザのアドレス欄に打ち込んで、サイトが出るか確認します。
2. **スマホでも開いてみてください。**（自分のスマホでこのURLを開く）
3. スマホで、ポケモンのスクリーンショット読み取りが動くかも試してみてください。

うまく出ないときは、下の「困ったときのチェックリスト」を見てください。

---

## あとから中身を直したいとき

パソコン側で直したあと、**GitHub に上げるだけ**で、公開中のサイトにも反映されます。

1. Claude に「直して」と頼む（または自分で `mytools/template.html` を直す）
2. `pksl.teams` フォルダで `python mytools/build.py` を実行して `index.html` を作り直す
3. `git add -A` → `git commit` → `git push` で GitHub に上げる
4. **1〜2分待つ**と、公開中のサイトも新しくなります

> 見た目が古いままのときは、ブラウザで **Ctrl + Shift + R**（スマホなら
> ページを閉じてから開き直す）を押してください。前に読み込んだものが
> 残っているだけのことがあります。

---

## 検索結果に出したくなくなったら

`mytools/template.html` の上のほう（4行目あたり）に、次の1行を足します。

```html
<meta name="robots" content="noindex">
```

そのあと `python mytools/build.py` を実行して、`git push` すれば戻せます。
Claude に「検索避けを戻して」と言ってもらえれば、こちらでやります。

---

## 公開をやめたくなったら

Settings → Pages の画面で、**Source** を `None` に戻せば公開が止まります。
リポジトリを `Private`（非公開）に戻すこともできます（ステップ1と同じ場所です）。

---

## 困ったときのチェックリスト

| 症状 | 確認すること |
|---|---|
| URLを開くと「404」と出る | ・公開直後は数分かかります。5分ほど待って再読み込み<br>・ステップ2の「Branch」が `main` になっているか<br>・URLの最後の `/` を忘れていないか |
| Pages の画面に「Branch」の欄がない | ステップ1の「公開に切りかえる」が終わっていない可能性。もう一度 Settings のいちばん下を確認 |
| 画面は出るが真っ白 | ブラウザで **Ctrl + Shift + R**（キャッシュを無視して読み直す） |
| 文字が四角や「？」になる | `index.html` の文字コードが変わった可能性。`python mytools/build.py` で作り直して push し直す |
| スクリーンショット読み取りが動かない | 読み取りの部品をインターネットから取りに行きます。通信環境を確認してください |
